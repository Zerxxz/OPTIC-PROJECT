/**
 * Page entry: index (Overview / home)
 * Mounts chrome, then bootstraps any page-specific behaviour.
 */
import '@/assets/optic.css';
import { mountChrome } from '@/modules/chrome';

mountChrome();
